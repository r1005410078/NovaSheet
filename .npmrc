# Pin to npmjs registry so a contributor's global ~/.npmrc mirror
# (e.g. registry.npmmirror.com) doesn't leak into bun.lock and break
# CI runners that can't reach the mirror.
registry=https://registry.npmjs.org/
